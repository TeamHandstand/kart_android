CREATE TABLE team (
    id TEXT NOT NULL PRIMARY KEY,
    bronzeCount INTEGER,
    eventId TEXT,
    goldCount INTEGER,
    silverCount INTEGER,
    ribbonCount INTEGER,
    ranking INTEGER,
    updatedAt TEXT
);

select_all:
SELECT *
FROM team
WHERE id IS ?;

insert_row:
INSERT INTO team(id)
VALUES (?);