CREATE TABLE item (
    id TEXT NOT NULL PRIMARY KEY,
    imageUrl TEXT,
    displayableName TEXT,
    slug TEXT,
    attackType TEXT,
    challengeId TEXT,
    updatedAt TEXT
);

select_all:
SELECT *
FROM item
WHERE id=?;