CREATE TABLE course (
    id TEXT NOT NULL PRIMARY KEY,
    distance REAL DEFAULT 0,
    maxRegistrants INTEGER DEFAULT 0,
    name TEXT,
    startLat REAL DEFAULT 0,
    startLong REAL DEFAULT 0,
    updatedAt TEXT
);

select_all:
SELECT *
FROM course
WHERE id = ?;